(()=>{const q=(k=>{const s=window.location.search;if(!s)return"";if(s.includes("=")){const p=new URLSearchParams(s);return p.get("user")||p.get("u")||p.get("name")||p.get("username")||"";}return decodeURIComponent(s.slice(1));})();const $=id=>document.getElementById(id);const loader=$("loader");const inp=$("search-input");const avatarImg=$("avatar-img");const avatarSkel=$("avatar-skeleton");const displayNameEl=$("display-name");const usernameEl=$("username");const presenceEl=$("presence-pill");const bioEl=$("bio");const joinedEl=$("joined");const useridEl=$("userid");const verifiedEl=$("verified");const friendsEl=$("friends");const followersEl=$("followers");const followingEl=$("following");const statusEl=$("status");const blurbEl=$("blurb");const BASE_USERS="https://users.roproxy.com";const BASE_THUMB="https://thumbnails.roproxy.com";const BASE_FRIENDS="https://friends.roproxy.com";const BASE_PRESENCE="https://presence.roproxy.com";function showLoader(v){loader.classList.toggle("hidden",!v)}function showError(msg){const tmpl=document.getElementById("error-template");if(!tmpl)return;const node=tmpl.content.firstElementChild.cloneNode(true);node.querySelector("#error-text").textContent=msg;document.body.appendChild(node);setTimeout(()=>{node.remove()},3200)}async function safeJson(url,opts){try{const res=await fetch(url,opts);if(!res.ok)throw new Error("HTTP "+res.status);return await res.json()}catch(e){console.error("req fail",url,e);return null}}async function fetchUserByName(name){const body={usernames:[name],excludeBannedUsers:true};return await safeJson(BASE_USERS+"/v1/usernames/users",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(body)})}async function fetchUserCore(id){return await safeJson(BASE_USERS+"/v1/users/"+id)}async function fetchAvatarThumb(id){const d=await safeJson(BASE_THUMB+"/v1/users/avatar?userIds="+id+"&size=352x352&format=Png&isCircular=false");return d&&d.data&&d.data[0]?d.data[0].imageUrl:null}async function fetchCounts(id){const [friends,followers,following]=await Promise.all([safeJson(BASE_FRIENDS+"/v1/users/"+id+"/friends/count"),safeJson(BASE_FRIENDS+"/v1/users/"+id+"/followers/count"),safeJson(BASE_FRIENDS+"/v1/users/"+id+"/followings/count")]);return{friends:friends&&friends.count||0,followers:followers&&followers.count||0,following:following&&following.count||0}}async function fetchPresence(id){const d=await safeJson(BASE_PRESENCE+"/v1/presence/users",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({userIds:[id]})});if(!d||!d.userPresences||!d.userPresences[0])return null;return d.userPresences[0]}function setPresence(p){presenceEl.classList.remove("online","ingame","offline");if(!p){presenceEl.textContent="Unknown";return}if(p.userPresenceType===2){presenceEl.textContent="Online";presenceEl.classList.add("online")}else if(p.userPresenceType===3){presenceEl.textContent=p.lastLocation||"In-Game";presenceEl.classList.add("ingame")}else{presenceEl.textContent="Offline";presenceEl.classList.add("offline")}}function fmtDate(iso){if(!iso)return"—";const d=new Date(iso);if(!+d)return"—";return d.toLocaleDateString(undefined,{year:"numeric",month:"short",day:"numeric"})}async function loadProfile(name){if(!name){showLoader(false);return}showLoader(true);inp.value=name;avatarImg.style.display="none";avatarSkel.style.display="block";displayNameEl.textContent="Loading…";usernameEl.textContent="@"+name;bioEl.textContent="";joinedEl.textContent="—";useridEl.textContent="—";verifiedEl.textContent="—";friendsEl.textContent=followersEl.textContent=followingEl.textContent="—";statusEl.textContent="—";blurbEl.textContent="—";setPresence(null);const uLookup=await fetchUserByName(name);if(!uLookup||!uLookup.data||!uLookup.data.length){showLoader(false);showError("User not found");displayNameEl.textContent="User not found";return}const u=uLookup.data[0];const uid=u.id;useridEl.textContent=uid;displayNameEl.textContent=u.displayName||u.name;usernameEl.textContent="@"+u.name;verifiedEl.textContent=u.hasVerifiedBadge?"Yes":"No";const [core,thumbUrl,counts,pres]=await Promise.all([fetchUserCore(uid),fetchAvatarThumb(uid),fetchCounts(uid),fetchPresence(uid)]);if(core){bioEl.textContent=core.description||"No description set.";joinedEl.textContent=fmtDate(core.created);statusEl.textContent=core.status||"No status.";blurbEl.textContent=core.description?core.description.slice(0,220):"—"}if(thumbUrl){avatarImg.src=thumbUrl;avatarImg.onload=()=>{avatarSkel.style.display="none";avatarImg.style.display="block"};avatarImg.onerror=()=>{avatarSkel.style.display="block";avatarImg.style.display="none"}}if(counts){friendsEl.textContent=counts.friends.toLocaleString();followersEl.textContent=counts.followers.toLocaleString();followingEl.textContent=counts.following.toLocaleString()}setPresence(pres);showLoader(false)}document.getElementById("search").addEventListener("submit",e=>{e.preventDefault();const v=inp.value.trim();if(!v)return;const base=window.location.origin+window.location.pathname;window.location.href=base+"?"+encodeURIComponent(v)});document.querySelectorAll(".tab").forEach(btn=>{btn.addEventListener("click",()=>{document.querySelectorAll(".tab").forEach(b=>b.classList.remove("active"));btn.classList.add("active");const t=btn.dataset.tab;document.querySelectorAll(".panel").forEach(p=>p.classList.remove("active"));const panel=document.getElementById("panel-"+t);if(panel)panel.classList.add("active")})});if(q){loadProfile(q);}else{showLoader(false);inp.focus();}})();
